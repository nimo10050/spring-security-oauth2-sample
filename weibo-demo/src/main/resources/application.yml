server:
  port: 8081
  servlet:
    context-path: /weibo

#thymeleaf
#默认到resource/templates目录下寻找

spring:
  thymeleaf:
    mode: HTML5
    encoding: utf-8
    servlet:
      content-type: text/html
    prefix: classpath:/templates/
    cache: false
